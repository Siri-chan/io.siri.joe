package io.siri.joe;

/**
 * Virtual Annotation. Indicates that a function does not require a super() call when overridden.
 * Acts like an abstract class in practise, but with an empty declaration, rather than no declaration.
 *
 * @author Siri
 */
public @interface Virtual {}
